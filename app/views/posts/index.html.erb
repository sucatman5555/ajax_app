<h1>AjaxApp</h1>
<%= form_with url:  "/posts", method: :post,id: "form" do |form| %>
  <%# contentを取得するために、index.html.erbに下記のハイライトのようにid:"content"を追記してください。%>
  <%#= form.text_field :content %>
  <%= form.text_field :content  , id: "content"%>
  
  <%= form.submit '投稿する' , id: "submit" %>
<% end %>

<%# 投稿後にメモを追加する要素として<div id="list"></div>を追記 %>
<div id="list">
</div>

<% @posts.each do |post| %>
<%# 
 この部分を「クリックした時」にイベントを発火させたいので、クリックされる部分の要素を取得するためにchecked.jsへ下記のように記述します。 
>
<%#
  data-idとdata-checkのカスタムデータ属性を追加しました。
  前者はメモのidが入り、後者は既読の有無を管理するcheckedカラムの情報が入ります。
 <div class="post">  変更前
%>
<div class="post" data-id=<%= post.id %> data-check=<%= post.checked %>>
  <div class="post-date">
    投稿日時：<%= post.created_at %>
  </div>
  <div class="post-content">
    <%= post.content %>
  </div>
</div>
<%#  end %>
<% end %>